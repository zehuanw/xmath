namespace xmath{
/*
 * Generate numbers 
 */
template<typename T_> 
struct SeqPolicy{
  static T_ gen(int idx, std::pair<T_> limits){
    
  }
};

template<typename T_>
struct RandomPolicy{
  static T_ gen(int idx, std::pair<T_> limits){

  }
};


} //namespace xmath
